# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=false
kotlin.code.style=official
# Suppress warning for compileSdk 34
android.suppressUnsupportedCompileSdk=34

# 开启 Gradle 构建缓存
org.gradle.caching=true
# 开启并行构建
org.gradle.parallel=true
# 开启配置按需加载
org.gradle.configureondemand=true
# 开启守护进程，提高构建性能
org.gradle.daemon=true

# 设置网络超时
org.gradle.internal.http.connectionTimeout=5000
org.gradle.internal.http.socketTimeout=5000

# KSP支持增量编译
ksp.incremental=true
ksp.incremental.intermodule=true

# 解决jlink错误问题
android.jetifier.ignorelist=bcprov

# 腾讯云 Maven 仓库镜像 - 暂时注释掉，解决构建问题
# systemProp.gradle.wrapperUser=
# systemProp.gradle.wrapperPassword=
# systemProp.http.proxyHost=mirrors.cloud.tencent.com
# systemProp.https.proxyHost=mirrors.cloud.tencent.com 